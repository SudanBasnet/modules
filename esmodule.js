export const add = (a, b) => {
  return a + b;
};
//named export
export const sub = (a, b) => {
  return a - b;
};

// export { add, sub }; => named export for exporting both at once
// export default add;
